import 'package:flutter/material.dart';

/// Paleta de cores oficial do TrainUp.
class AppColors {
  AppColors._();

  /// Azul escuro — cor primária, usada em fundos de destaque e textos fortes.
  static const navy = Color(0xFF0B2545);

  /// Verde — cor de ação/progresso (CTAs, indicadores de evolução).
  static const green = Color(0xFF1FD65F);

  /// Amarelo — acento sutil (bandeira do Brasil), uso pontual em destaques.
  static const yellow = Color(0xFFFFD60A);

  static const black = Color(0xFF0A0A0A);
  static const white = Color(0xFFFFFFFF);

  /// Fundo neutro das telas de conteúdo.
  static const background = Color(0xFFF5F7FA);

  /// Texto secundário sobre fundo claro.
  static const grey = Color(0xFF6B7785);
}
